<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.logistics.api.mapper.AddressMapper">

    <resultMap id="BaseResultMap" type="com.logistics.api.model.Address">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="contactName" column="contact_name"/>
        <result property="contactPhone" column="contact_phone"/>
        <result property="province" column="province"/>
        <result property="city" column="city"/>
        <result property="district" column="district"/>
        <result property="detailAddress" column="detail_address"/>
        <result property="latitude" column="latitude"/>
        <result property="longitude" column="longitude"/>
        <result property="isDefault" column="is_default"/>
        <result property="tag" column="tag"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, contact_name, contact_phone, province, city, district, detail_address, latitude, longitude, is_default, tag, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM addresses WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM addresses WHERE user_id = #{userId} ORDER BY is_default DESC, create_time DESC
    </select>

    <select id="selectDefaultByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM addresses WHERE user_id = #{userId} AND is_default = 1 LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO addresses (user_id, contact_name, contact_phone, province, city, district, detail_address, latitude, longitude, is_default, tag, create_time, update_time)
        VALUES (#{userId}, #{contactName}, #{contactPhone}, #{province}, #{city}, #{district}, #{detailAddress}, #{latitude}, #{longitude}, #{isDefault}, #{tag}, NOW(), NOW())
    </insert>

    <update id="updateById">
        UPDATE addresses
        SET contact_name = #{contactName},
            contact_phone = #{contactPhone},
            province = #{province},
            city = #{city},
            district = #{district},
            detail_address = #{detailAddress},
            latitude = #{latitude},
            longitude = #{longitude},
            is_default = #{isDefault},
            tag = #{tag},
            update_time = NOW()
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <delete id="deleteById">
        DELETE FROM addresses WHERE id = #{id} AND user_id = #{userId}
    </delete>

    <update id="clearDefaultByUserId">
        UPDATE addresses SET is_default = 0 WHERE user_id = #{userId}
    </update>

    <update id="setDefaultById">
        UPDATE addresses SET is_default = 1 WHERE id = #{id} AND user_id = #{userId}
    </update>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM addresses WHERE user_id = #{userId}
    </select>

</mapper>
