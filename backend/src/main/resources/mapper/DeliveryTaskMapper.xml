<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.logistics.api.mapper.DeliveryTaskMapper">

    <resultMap id="BaseResultMap" type="com.logistics.api.model.DeliveryTask">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="courierId" column="courier_id"/>
        <result property="status" column="status"/>
        <result property="acceptTime" column="accept_time"/>
        <result property="pickupTime" column="pickup_time"/>
        <result property="deliveryTime" column="delivery_time"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_id, courier_id, status, accept_time, pickup_time, delivery_time, create_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM delivery_tasks WHERE id = #{id}
    </select>

    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM delivery_tasks WHERE order_id = #{orderId}
    </select>

    <select id="selectAllByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM delivery_tasks WHERE order_id = #{orderId} ORDER BY create_time DESC
    </select>

    <select id="selectLatestByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM delivery_tasks WHERE order_id = #{orderId} ORDER BY create_time DESC LIMIT 1
    </select>

    <select id="selectByCourierIdAndOrderId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM delivery_tasks WHERE courier_id = #{courierId} AND order_id = #{orderId} ORDER BY create_time DESC LIMIT 1
    </select>

    <select id="selectByCourierId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM delivery_tasks WHERE courier_id = #{courierId} ORDER BY create_time DESC
    </select>

    <select id="selectByCourierIdAndStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM delivery_tasks WHERE courier_id = #{courierId} AND status = #{status} ORDER BY create_time DESC
    </select>

    <select id="selectByCourierIdAndStatuses" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM delivery_tasks 
        WHERE courier_id = #{courierId} 
        AND status IN 
        <foreach collection="statuses" item="status" open="(" separator="," close=")">
            #{status}
        </foreach>
        ORDER BY create_time DESC
    </select>

    <select id="selectByCourierIdsAndStatuses" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM delivery_tasks
        WHERE courier_id IN
        <foreach collection="courierIds" item="cid" open="(" separator="," close=")">
            #{cid}
        </foreach>
        AND status IN
        <foreach collection="statuses" item="status" open="(" separator="," close=")">
            #{status}
        </foreach>
        ORDER BY courier_id, create_time DESC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM delivery_tasks ORDER BY create_time DESC
    </select>

    <select id="countTotalTasks" resultType="long">
        SELECT COUNT(*) FROM delivery_tasks
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(*) FROM delivery_tasks WHERE `status` = #{status}
    </select>

    <select id="countDeliveredByDeliveryTimeBetween" resultType="long">
        SELECT COUNT(*) FROM delivery_tasks
        WHERE `status` = 'delivered'
        AND delivery_time &gt;= #{startTime} AND delivery_time &lt; #{endTime}
    </select>

    <select id="selectTopCouriersByDeliveredCount" resultType="map">
        SELECT courier_id AS courierId, COUNT(*) AS completedTasks
        FROM delivery_tasks
        WHERE `status` = 'delivered' AND courier_id IS NOT NULL
        GROUP BY courier_id
        ORDER BY completedTasks DESC
        LIMIT #{limit}
    </select>

    <select id="selectByCourierIdAndCreateTimeBetween" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM delivery_tasks
        WHERE courier_id = #{courierId}
        AND create_time &gt;= #{startTime} AND create_time &lt;= #{endTime}
        ORDER BY create_time DESC
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM delivery_tasks WHERE status = #{status} ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO delivery_tasks (order_id, courier_id, status, create_time, accept_time)
        VALUES (#{orderId}, #{courierId}, #{status}, NOW(), NOW())
    </insert>

    <update id="updateStatus">
        UPDATE delivery_tasks SET status = #{status} WHERE id = #{id}
    </update>

    <update id="updateAcceptTime">
        UPDATE delivery_tasks SET accept_time = NOW() WHERE id = #{id}
    </update>

    <update id="updatePickupTime">
        UPDATE delivery_tasks SET pickup_time = NOW() WHERE id = #{id}
    </update>

    <update id="updateDeliveryTime">
        UPDATE delivery_tasks SET delivery_time = NOW() WHERE id = #{id}
    </update>

</mapper>
