# 基于App的物流管理和追踪平台 —— 部署操作指南

> 本文档指导你逐步完成项目的生产环境部署，并标注了论文 5.3 节所需截图的拍摄位置。

---

## 准备工作

部署前请确保你已具备以下条件：

| 事项 | 说明 |
|---|---|
| 阿里云账号 | 已完成实名认证（推荐注册一个，学生有免费额度） |
| 域名 | 一个已备案的域名（如 `logistics.yourdomain.com`） |
| Docker 环境 | 本地安装 Docker Desktop（Windows/Mac）或 Docker Engine（Linux） |
| JDK 17 + Maven | 本地已具备编译能力（开发时已有则跳过） |
| Git | 用于推送代码 |

---

## 第一步：编写 Dockerfile 并本地验证

### 1.1 创建 Dockerfile

在项目 `backend/` 目录下创建 `Dockerfile` 文件，内容如下：

```dockerfile
FROM maven:3.9-eclipse-temurin-17 AS build
WORKDIR /app
COPY pom.xml .
RUN mvn dependency:go-offline -B
COPY src ./src
RUN mvn package -DskipTests -B

FROM eclipse-temurin:17-jre-alpine AS runtime
WORKDIR /app
COPY --from=build /app/target/*.jar app.jar
EXPOSE 8082
ENTRYPOINT ["java", "-jar", "app.jar"]
```

### 1.2 本地构建并测试

在 `backend/` 目录下运行以下命令：

```bash
# 构建 Docker 镜像
docker build -t logistics-api:1.0.0 .

# 运行容器测试（使用开发环境配置）
docker run -d --name logistics-test -p 8082:8082 logistics-api:1.0.0

# 验证服务是否启动成功
curl http://localhost:8082/api/
```

测试通过后停止并删除测试容器：

```bash
docker stop logistics-test
docker rm logistics-test
```

> **📸 【拍照点1 —— 对应论文图5-8 上半部分】**
> 
> 截图内容：终端中 Docker 构建成功（`docker build` 命令输出 "BUILD SUCCESS"）的界面。
> 截图中应能看到 `FROM maven...` 两阶段的构建日志、`docker build -t logistics-api:1.0.0 .` 命令和最终的成功提示。
> 建议截图后裁剪掉无用的长路径信息，保留关键日志即可。

---

## 第二步：在阿里云上创建云资源

### 2.1 创建 ECS 云服务器

1. 登录 [阿里云控制台](https://home.console.aliyun.com/)
2. 进入「云服务器 ECS」→「实例」→「创建实例」
3. 配置如下：

| 配置项 | 推荐值 |
|---|---|
| 付费方式 | 按量付费（测试用）/ 包年包月（正式用） |
| 地域 | 华北2（北京），与 OSS 同地域 |
| 实例规格 | 2核4GB（ecs.c7.large 或同等） |
| 操作系统 | Alibaba Cloud Linux 3 |
| 系统盘 | 40GB ESSD |
| 公网IP | 分配公网 IPv4 地址 |

4. 在「安全组」中添加入方向规则：

| 端口 | 协议 | 来源 | 用途 |
|---|---|---|---|
| 22 | TCP | 你的IP | SSH 远程管理 |
| 80 | TCP | 0.0.0.0/0 | HTTP 访问 |
| 443 | TCP | 0.0.0.0/0 | HTTPS 访问 |

5. 创建完成后，记录下 **公网IP地址**（如 `47.xx.xx.xx`）

> **📸 【拍照点2 —— 对应论文图5-7 上半部分】**
>
> 截图内容：ECS 实例列表页面，能看到实例名称、公网IP、状态为「运行中」、配置信息。
> 建议打开 ECS 实例详情页，截取包含「实例规格」「公网IP」「镜像」信息的部分。

### 2.2 创建 RDS MySQL 数据库

1. 进入「云数据库 RDS」→「创建实例」
2. 配置：

| 配置项 | 推荐值 |
|---|---|
| 数据库类型 | MySQL 8.0 |
| 系列 | 基础版（测试）/ 高可用版（生产） |
| 规格 | 1核2GB（测试足够） |
| 存储 | 20GB |
| 地域 | 与 ECS 同地域（华北2-北京） |

3. 创建完成后：
   - 进入实例详情 →「账号管理」→ 创建数据库账号（用户名 `logistics`，设置一个强密码并记下来）
   - 进入「数据库管理」→ 创建数据库 `logistics_db`
   - 进入「白名单与安全组」→ 添加 ECS 的**内网IP**到白名单（或直接添加 ECS 所在安全组）
   - 记下数据库的**内网连接地址**（如 `rm-xxx.mysql.rds.aliyuncs.com`）

4. 导入数据表结构（在本地执行）：

```bash
# 导出本地开发环境的表结构（如果还没有独立schema.sql）
# 已经在 backend/src/main/resources/db/schema.sql 中

# 使用 mysql 客户端连接 RDS 导入
mysql -h rm-xxx.mysql.rds.aliyuncs.com -u logistics -p logistics_db < backend/src/main/resources/db/schema.sql
mysql -h rm-xxx.mysql.rds.aliyuncs.com -u logistics -p logistics_db < backend/src/main/resources/db/data.sql
```

### 2.3 创建 Redis 实例

1. 进入「云数据库 Redis」→「创建实例」
2. 配置：

| 配置项 | 推荐值 |
|---|---|
| 版本 | Redis 6.0 |
| 类型 | 主从版（测试）/ 集群版（生产） |
| 规格 | 1GB 标准版 |
| 地域 | 与 ECS 同地域 |

3. 创建后记下 **内网连接地址**

### 2.4 创建 RocketMQ 实例（替代 RabbitMQ）

> **注意**：你的项目原用 RabbitMQ，但阿里云托管版 RabbitMQ 价格较高。
> 推荐改用阿里云 RocketMQ 5.0（价格更低，功能更强），或继续在 ECS 上自行部署 RabbitMQ。
> 
> **方案A（推荐）**：创建 RocketMQ 实例，后续代码中切换消息队列客户端
> 
> **方案B（简单）**：在 ECS 上用 Docker 部署 RabbitMQ（见 4.3 节）
>
> 下面按方案B操作，最简单省事。

> **📸 【拍照点3 —— 对应论文图5-7 下半部分】**
>
> 截图内容：阿里云控制台概览页，能看到 ECS、RDS、Redis 三个实例的列表。
> 建议在控制台首页将 ECS、RDS、Redis 添加到「我的资源」后截图，清晰展示各资源状态均为「运行中」。
> 或者在「资源管理」页面中截取包含所有资源一览的视图。
>
> 如果有多页，可合成一张图或者分别截图后拼图。

---

## 第三步：准备生产环境配置文件

在 `backend/src/main/resources/` 下创建 `application-prod.yml`：

```yaml
server:
  port: 8082
  servlet:
    context-path: /api
  address: 0.0.0.0

spring:
  datasource:
    url: jdbc:mysql://rm-xxx.mysql.rds.aliyuncs.com:3306/logistics_db?useSSL=true&serverTimezone=UTC
    username: logistics
    password: 你设置的数据库密码
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: r-xxx.redis.rds.aliyuncs.com    # Redis 内网地址
      port: 6379
      password: 你设置的Redis密码
      database: 0
      timeout: 60000
      lettuce:
        pool:
          max-active: 8
          max-wait: -1
          max-idle: 8
          min-idle: 0

  rabbitmq:
    host: 127.0.0.1     # RabbitMQ 部署在同一台 ECS 上
    port: 5672
    username: admin
    password: admin123   # 建议改成更强的密码
    virtual-host: /
    connection-timeout: 15000
    publisher-confirm-type: correlated
    publisher-returns: true
    template:
      mandatory: true
    listener:
      simple:
        concurrency: 5
        max-concurrency: 10
        prefetch: 1
        acknowledge-mode: auto
        default-requeue-rejected: false

  # 生产环境日志配置
  profiles:
    active: prod

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.logistics.api.model
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

jwt:
  access:
    secret: 在此生成一个新的32位以上密钥
    expiration: 3600000
  refresh:
    secret: 在此生成另一个新的32位以上密钥
    expiration: 604800000

file:
  upload:
    path: /app/uploads/
  access:
    url: /api/uploads/

dispatch:
  rules:
    max-orders-per-courier: 5
    search-radius-km: 10
    expand-radius-km: 20
    auto-expand: true
    route-penalty-weight: 10
    direct-distance-weight: 1
    load-penalty-km-per-order: 1.5
    max-candidates-for-detour-scoring: 32

app:
  order:
    auto-exception:
      enabled: true
      paid-not-in-pool-minutes: 45
      grab-pool-stale-minutes: 120
      awaiting-courier-confirm-minutes: 60
  test-mode: false    # 生产环境关闭测试模式！

aliyun:
  oss:
    enabled: true
    endpoint: oss-cn-beijing.aliyuncs.com    # 与生产环境地域一致
    bucket-name: wubysj
    access-key-id: 你的AccessKey    # 建议使用 RAM 子账号，不要用主账号
    access-key-secret: 你的AccessKeySecret
```

> ⚠️ **安全提醒**：生产环境不要使用开发环境中的默认密码和密钥。JWT 密钥可以用以下命令生成：
> ```bash
> openssl rand -base64 32
> ```

---

## 第四步：登录 ECS 并部署服务

### 4.1 SSH 登录到 ECS

```bash
ssh root@47.xx.xx.xx    # 替换为你的 ECS 公网IP
```

### 4.2 在 ECS 上安装 Docker

```bash
# 安装 Docker
curl -fsSL https://get.docker.com | bash

# 启动 Docker 并设置开机自启
systemctl start docker
systemctl enable docker

# 验证安装
docker --version
```

### 4.3 在 ECS 上部署 RabbitMQ（使用 Docker）

```bash
docker run -d \
  --name rabbitmq \
  --restart always \
  -p 5672:5672 \
  -p 15672:15672 \
  -e RABBITMQ_DEFAULT_USER=admin \
  -e RABBITMQ_DEFAULT_PASS=admin123 \
  rabbitmq:3.12-management-alpine
```

### 4.4 构建并推送后端镜像

在**本地** `backend/` 目录下，将 application-prod.yml 打进镜像（修改 Dockerfile 或使用挂载方式）：

**方式一（推荐：配置挂载）**：镜像中保留默认配置，运行时挂载生产配置。

```bash
# 在本地 backend/ 目录下构建镜像（挂载方式，不需要修改 Dockerfile）
docker build -t logistics-api:1.0.0 .

# 登录阿里云容器镜像服务 ACR
# 进入阿里云控制台 → 容器镜像服务 ACR → 创建个人版实例 → 创建镜像仓库
# 仓库名称：logistics-api，类型：私有
docker login --username=你的阿里云账号 registry.cn-beijing.aliyuncs.com

# 打标签
docker tag logistics-api:1.0.0 registry.cn-beijing.aliyuncs.com/你的命名空间/logistics-api:1.0.0

# 推送到 ACR
docker push registry.cn-beijing.aliyuncs.com/你的命名空间/logistics-api:1.0.0
```

> **📸 【拍照点4 —— 对应论文图5-8 下半部分】**
>
> 截图内容：阿里云 ACR 镜像仓库页面中已上传的镜像列表，能看到镜像版本号和推送时间。
> 或者截图 Docker Desktop 中 Images 标签页，能看到 `logistics-api:1.0.0` 镜像，以及推送成功后的 ACR 仓库页面。
> 如果不好截 ACR 页面，可以截终端中 `docker push` 成功的输出。

### 4.5 在 ECS 上拉取并运行容器

```bash
# 回到 ECS 终端，登录 ACR
docker login --username=你的阿里云账号 registry.cn-beijing.aliyuncs.com

# 拉取镜像
docker pull registry.cn-beijing.aliyuncs.com/你的命名空间/logistics-api:1.0.0

# 在 ECS 上创建生产配置目录
mkdir -p /opt/logistics/config

# 将 application-prod.yml 上传到 ECS
# （在本地执行，将配置文件 scp 到 ECS）
scp backend/src/main/resources/application-prod.yml root@47.xx.xx.xx:/opt/logistics/config/application.yml

# 回到 ECS 终端，运行容器
docker run -d \
  --name logistics-api \
  --restart always \
  -p 8082:8082 \
  -v /opt/logistics/config/application.yml:/app/config/application.yml \
  registry.cn-beijing.aliyuncs.com/你的命名空间/logistics-api:1.0.0

# 查看容器日志，确认启动成功
docker logs -f logistics-api

# 测试 API
curl http://localhost:8082/api/
```

---

## 第五步：配置 Nginx 反向代理 + SSL

### 5.1 部署 Nginx

在 ECS 上执行：

```bash
# 创建 Nginx 配置目录
mkdir -p /opt/nginx/conf.d
mkdir -p /opt/nginx/ssl
mkdir -p /opt/nginx/html

# 运行 Nginx 容器
docker run -d \
  --name nginx \
  --restart always \
  -p 80:80 \
  -p 443:443 \
  -v /opt/nginx/conf.d:/etc/nginx/conf.d \
  -v /opt/nginx/ssl:/etc/nginx/ssl \
  -v /opt/nginx/html:/usr/share/nginx/html \
  nginx:alpine
```

### 5.2 配置 Nginx 反向代理

在 ECS 上编辑 `/opt/nginx/conf.d/logistics.conf`：

```nginx
# HTTP → HTTPS 强制跳转
server {
    listen 80;
    server_name logistics.yourdomain.com;   # 替换为你的域名

    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS 主配置
server {
    listen 443 ssl http2;
    server_name logistics.yourdomain.com;   # 替换为你的域名

    ssl_certificate     /etc/nginx/ssl/logistics.pem;
    ssl_certificate_key /etc/nginx/ssl/logistics.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # 反向代理到后端 Spring Boot
    location /api/ {
        proxy_pass http://127.0.0.1:8082;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }

    # WebSocket 代理（物流实时追踪功能的核心配置）
    location /api/stomp {
        proxy_pass http://127.0.0.1:8082;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 3600s;
    }

    location /api/ws {
        proxy_pass http://127.0.0.1:8082;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 3600s;
    }
}
```

重载 Nginx 配置：

```bash
docker exec nginx nginx -t          # 检查配置语法
docker exec nginx nginx -s reload   # 重载配置
```

> **📸 【拍照点5 —— 对应论文图5-9】**
>
> 截图内容：Nginx 配置文件 `logistics.conf` 的代码界面，清晰展示反向代理规则和 WebSocket 配置。
> 建议在 VS Code 或文本编辑器中打开该文件截图，代码高亮清晰可见。
> 同时可配合一张浏览器访问 `https://你的域名/api/` 返回正常响应的截图。

### 5.3 配置 SSL 证书

在阿里云控制台「SSL 证书」→「免费证书」中申请（免费，每年续期一次）：

1. 点击「创建证书」→ 选择「免费证书（个人DV）」
2. 填写域名 `logistics.yourdomain.com`
3. 验证域名所有权（DNS 验证：在域名 DNS 处添加 TXT 记录）
4. 审核通过后下载 Nginx 格式的证书
5. 将证书文件上传到 ECS：

```bash
# 在本地执行
scp logistics.pem root@47.xx.xx.xx:/opt/nginx/ssl/
scp logistics.key root@47.xx.xx.xx:/opt/nginx/ssl/
```

6. 重载 Nginx 使证书生效

### 5.4 配置域名 DNS 解析

在你的域名 DNS 管理页面（阿里云 DNS 或你使用的其他 DNS 提供商）：

| 记录类型 | 主机记录 | 记录值 |
|---|---|---|
| A | logistics（或@） | ECS 的公网IP（47.xx.xx.xx） |

等待 DNS 生效后（通常几分钟），访问 `https://logistics.yourdomain.com/api/` 验证。

---

## 第六步：Android 客户端构建

### 6.1 修改 API 地址

在前端项目中，找到 API 基础地址配置，将开发环境的地址（如 `http://localhost:8082/api/` 或 `http://10.0.2.2:8082/api/`）替换为：

```
https://logistics.yourdomain.com/api/
```

### 6.2 构建 Release APK

在 Android 前端项目目录下：

```bash
cd android
./gradlew assembleRelease
```

构建完成后，APK 文件位于：
```
android/app/build/outputs/apk/release/app-release.apk
```

> 注意：首次构建 Release 需要先生成签名密钥（keystore），在 `android/app/` 下执行：
> ```bash
> keytool -genkey -v -keystore logistics-release.keystore \
>   -alias logistics -keyalg RSA -keysize 2048 -validity 10000
> ```
> 然后在 `android/gradle.properties` 中配置签名信息。

> **📸 【拍照点6 —— 对应论文图5-10】**
>
> 截图内容：Android Studio 或终端中 Gradle 构建成功的界面，能看到 BUILD SUCCESSFUL 和 APK 输出路径。
> 建议同时截一张 APK 文件在文件管理器中的截图，显示文件大小和生成时间。
> 也可以截一张手机安装 APK 后的 App 首页作为补充。

---

## 截图清单汇总

| 论文编号 | 内容 | 拍摄位置 | 建议来源 |
|---|---|---|---|
| 图5-6 | 系统部署整体架构图 | 自行用 Visio/draw.io 绘制 | 架构图（参考下方模板） |
| 图5-7 | 阿里云资源创建——控制台概览 | 第二步 | 阿里云控制台截图 |
| 图5-8 | 后端服务镜像构建与容器部署 | 第一步 + 第四步 | 终端截图 + ACR 截图 |
| 图5-9 | HTTP访问服务——Nginx反向代理与SSL配置 | 第五步 | Nginx 配置代码截图 |
| 图5-10 | Android客户端构建与分发 | 第六步 | 构建成功截图 + APK截图 |

### 图5-6 架构图绘制参考

用 draw.io / Visio / ProcessOn 等工具绘制，包含以下元素：

```
┌──────────────────────────────────────┐
│            用户（Android App）         │
└──────────────────┬───────────────────┘
                   │ HTTPS (443)
                   ▼
┌──────────────────────────────────────┐
│         阿里云 ECS 云服务器             │
│  ┌────────────────────────────────┐  │
│  │  Nginx (反向代理 + SSL终止)      │  │
│  │  :80 → 301 redirect → :443      │  │
│  │  /api/* → :8082                 │  │
│  │  /api/stomp→ WebSocket代理      │  │
│  └───────────┬────────────────────┘  │
│              │                       │
│  ┌───────────▼────────────────────┐  │
│  │  Spring Boot 容器 (Docker)      │  │
│  │  端口: 8082                     │  │
│  └────────────────────────────────┘  │
│              │                       │
│  ┌───────────▼────────────────────┐  │
│  │  RabbitMQ 容器 (Docker)         │  │
│  │  端口: 5672                     │  │
│  └────────────────────────────────┘  │
└──────────┬──────────┬────────────────┘
           │ 内网      │ 公网
           ▼           ▼
┌──────────────┐ ┌──────────────┐
│  RDS MySQL   │ │  OSS + CDN   │
│  (云数据库)   │ │  (对象存储)   │
└──────────────┘ └──────────────┘
           ▲
┌──────────────┐
│  Redis 云缓存 │
└──────────────┘
```

---

## 常见问题排查

| 问题 | 排查方法 |
|---|---|
| Docker 容器启动后立刻退出 | `docker logs logistics-api` 查看日志，通常是数据库连接失败 |
| API 返回 502 | 检查 Nginx 能否访问 `127.0.0.1:8082`：`curl http://127.0.0.1:8082/api/` |
| WebSocket 连接失败 | 检查 Nginx 是否配置了 Upgrade 头，确认 `/api/stomp` 路径代理规则正确 |
| SSL 证书无效 | 检查证书是否过期，路径是否正确，`docker exec nginx nginx -t` 检查语法 |
| 端口不通 | 检查阿里云安全组是否放行了 80 和 443 端口 |

---

## 成本预估（阿里云按量付费）

| 资源 | 规格 | 月费（约） |
|---|---|---|
| ECS 2核4GB | 按量 | ¥200-300 |
| RDS MySQL 1核2GB | 基础版 | ¥100-150 |
| Redis 1GB | 标准版 | ¥100-120 |
| OSS + CDN | 按量 | ¥5-20 |
| 弹性公网IP | 按量 | ¥10-15 |
| **合计** | | **约 ¥400-600/月** |

> 注意：学生认证后可领取阿里云免费额度（通常包含数月免费 ECS），可大幅降低测试成本。

---

*最后更新：2026年5月*