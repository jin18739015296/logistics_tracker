### 第 5 章 基于App的物流管理和追踪平台实现

本章对基于App的物流管理和追踪平台进行实现工作，完成系统设计阶段所规划的各项目标。系统后端基于Spring Boot 3.2框架，使用Java 17语言，按照控制器-服务-数据访问的分层结构实现系统功能，通过RESTful API与Android客户端进行数据交互。系统采用MySQL作为关系型数据库，使用Redis提供缓存与分布式锁能力，通过RabbitMQ消息中间件处理异步消息，借助WebSocket实现服务端到客户端的实时数据推送。本章节通过界面截图、代码片段等形式，描述系统主要功能的实现过程。

## 5.1 项目整体配置实现

### 5.1.1 后端项目配置

系统后端基于Apache Maven进行项目构建和依赖管理，项目对象模型（POM）配置文件pom.xml中声明了项目所依赖的全部组件。项目以spring-boot-starter-parent 3.2.0作为父级依赖，统一管理Spring Boot生态内各组件的版本号。核心依赖包括：spring-boot-starter-web提供Web MVC和RESTful API支持，mybatis-spring-boot-starter 3.0.3实现数据库访问层的映射，spring-boot-starter-security和jjwt 0.12.5提供安全认证与JWT令牌支持，spring-boot-starter-websocket实现WebSocket实时通信，spring-boot-starter-data-redis接入Redis缓存，spring-boot-starter-amqp接入RabbitMQ消息队列，以及aliyun-sdk-oss 3.17.4实现阿里云对象存储的文件上传功能。POM中依赖配置的核心部分如源代码5-1所示。

源代码5-1 后端项目POM依赖配置（核心部分）

```xml
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.2.0</version>
</parent>
<dependencies>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
        <groupId>org.mybatis.spring.boot</groupId>
        <artifactId>mybatis-spring-boot-starter</artifactId>
        <version>3.0.3</version>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt</artifactId>
        <version>0.12.5</version>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-websocket</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-amqp</artifactId>
    </dependency>
</dependencies>
```

### 5.1.2 应用配置实现

系统的各项运行参数和外部服务连接信息集中存放在application.yml配置文件中。该配置文件按功能模块分组组织，包含以下几个主要配置块：服务器配置——指定服务端口号和上下文路径；数据源配置——配置MySQL数据库的连接地址和认证凭证；Redis配置——配置缓存服务器的地址与连接池参数；RabbitMQ配置——配置消息中间件的连接信息、消息确认模式和消费者并发数；JWT配置——管理访问令牌和刷新令牌的签名密钥与过期时间；派单算法配置——定义自动派单的搜索半径、权重系数等规则参数。配置文件的核心内容如源代码5-2所示。

源代码5-2 应用配置文件application.yml（核心部分）

```yaml
server:
  port: 8082
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:13306/logistics_db?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
  data:
    redis:
      host: localhost
      port: 16379
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: admin
    password: admin
    publisher-confirm-type: correlated
```

服务注册方面，系统中的各项公共功能组件在Spring Boot的启动类中通过依赖注入的形式注册到容器中，为各业务模块提供统一的基础能力。安全管理方面，SecurityConfig类集中定义了接口的访问控制规则：对认证相关路径、文件上传的静态资源路径、WebSocket通信路径和第三方回调路径允许匿名访问，其余所有接口均要求请求方携带有效的JWT令牌。

## 5.2 系统功能实现

### 5.2.1 订单处理与异常恢复功能实现

订单是贯穿平台全部业务的核心实体，从用户下单创建到最终签收完成的整个生命周期中，既需要保证正常流转的高效有序，也需要对配送环节中可能出现的各类异常进行及时处置。前端采用React Native技术构建订单界面，以FlatList组件配合分类标签实现订单筛选展示。用户进入订单列表页面后，系统以卡片形式展示当前账号关联的全部订单，支持按"全部""待支付""进行中""待评价""已完成""已取消"六个分类标签进行前端筛选，每张订单卡片展示寄件人与收件人的摘要信息、当前状态标签、物品描述和运费金额，用户可点击进入订单追踪页面查看物流时间轴与实时地图。实现的界面如图5-1所示。

图5-1 用户端订单列表页面

订单正常流转的骨架是一套有限状态机模型。后端通过OrderStatus枚举统一定义了10个状态节点及其合法迁移路径，如源代码5-3所示。从待支付开始，用户完成支付后订单进入已支付状态等待分配配送员，随后依次经历待确认、待揽件、已揽件、运输中、已送达，最终由用户确认收货进入已完成。其中，EXCEPTION状态与正常配送状态处于同一模型中——待揽件、已揽件和运输中三种活跃配送状态均可转入异常，异常状态又可恢复至已支付或已揽件，体现了系统对配送中断场景的形式化建模。

源代码5-3 订单状态机流转规则实现

```java
public boolean canTransitionTo(OrderStatus target) {
    return switch (this) {
        case PENDING -> target == PAID || target == CANCELLED;
        case PAID -> target == AWAITING_COURIER_CONFIRM
                   || target == AWAITING_PICKUP || target == CANCELLED;
        case AWAITING_COURIER_CONFIRM -> target == AWAITING_PICKUP
                                       || target == PAID;
        case AWAITING_PICKUP -> target == PICKED_UP || target == EXCEPTION;
        case PICKED_UP -> target == IN_TRANSIT || target == EXCEPTION;
        case IN_TRANSIT -> target == DELIVERED || target == EXCEPTION;
        case DELIVERED -> target == COMPLETED;
        case COMPLETED, CANCELLED -> false;
        case EXCEPTION -> target == CANCELLED
                || target == PAID || target == PICKED_UP;
    };
}
```

从上述规则可以看到，已完成和已取消作为终态不允许进一步迁移。系统每次执行状态变更前均通过isValidTransition方法进行前置校验，校验不通过时抛出业务异常阻止操作执行，以类型约束的方式避免了非法状态跳转。

订单号的生成采用"LG + 年月日时分秒 + 4位随机数"的格式，利用时间戳保证时序可追溯性，末尾随机数确保并发场景下的全局唯一性，其实现如源代码5-4所示。

源代码5-4 订单号生成方法

```java
private String generateOrderNo() {
    String prefix = "LG";
    String dateStr = LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyyMMddHHmmss"));
    String randomStr = String.format("%04d", RANDOM.nextInt(10000));
    return prefix + dateStr + randomStr;
}
```

在正常流转过程中，系统对订单详情查询施加细粒度的权限控制——除管理员具有全局权限外，下单用户仅可查看自己的订单，配送员仅能查看分配给自己的订单，收件人在订单达到特定状态后可通过手机号匹配查看。同时，系统在订单送达前不向用户端暴露配送员的姓名和电话，仅在已送达或已完成之后才将这些隐私信息填充到返回的DTO中，保护配送员个人隐私。

然而实际配送中不可避免地会出现配送员长时间未响应、运输途中车辆故障或货品损毁等异常情况。为此，系统将异常状态纳入状态机统一管理后，进一步对异常本身进行了结构化分类。OrderExceptionType枚举定义了7种异常类型，按物品是否已交接给配送员和可恢复性两个维度划分为三大类，如源代码5-14所示。第一类为物品未交接的异常，包括"配送员未响应"和"揽件受阻"，此时物品仍在寄件人处，允许改派配送员或直接取消；第二类为物品已交接的异常，包括"揽件异常"和"运输异常"，物品已在配送员手上，揽件异常允许继续配送或取消退回，运输异常仅允许取消；第三类为不可恢复异常，包括"快件灭失""货品损毁"和"不可抗力"，仅允许取消并退款赔付。配送员通过前端异常上报页面提交异常类型和描述信息，界面如图5-11所示。

图5-11 配送员端异常上报页面

源代码5-14 异常类型分类枚举核心结构

```java
@Getter
public enum OrderExceptionType {
    // 物品未交接——可改派
    COURIER_NOT_RESPONDING("courier_not_responding", "配送员未响应", true,
            Set.of("reassign", "cancel")),
    PICKUP_PENDING_ISSUE("pickup_pending_issue", "揽件受阻", true,
            Set.of("reassign", "cancel")),
    // 物品已交接——可继续或取消
    PICKUP_ISSUE("pickup_issue", "揽件异常", true,
            Set.of("continue", "cancel")),
    TRANSIT_ISSUE("transit_issue", "运输异常", false,
            Set.of("cancel")),
    // 不可恢复——只能取消
    GOODS_LOST("goods_lost", "快件灭失", false, Set.of("cancel")),
    GOODS_DAMAGED("goods_damaged", "货品损毁", false, Set.of("cancel")),
    FORCE_MAJEURE("force_majeure", "不可抗力", false, Set.of("cancel"));

    private final String code;
    private final String title;
    private final boolean recoverable;
    private final Set<String> allowedActions;
}
```

每个枚举值通过`recoverable`标识是否可恢复正常配送，通过`allowedActions`集合声明允许的动作：reassign表示改派、continue表示继续配送、cancel表示取消。枚举同时提供`isActionAllowed`静态方法对恢复操作进行前置权限校验——cancel动作对全部类型开放，reassign仅对物品未交接类型开放，continue仅对揽件异常开放；未知类型默认放行所有动作以防止历史数据卡死。

异常订单的发现采用"主动上报 + 自动检测"双路径机制。配送员遇到问题时通过前端页面主动提交异常，后端CourierOrderServiceImpl的reportException方法校验操作权限后，若配送员未显式指定异常类型则根据订单当前状态自动推断——待确认状态推断为"配送员未响应"、已揽件状态推断为"揽件异常"、其余情况推断为"运输异常"。标记异常时同步更新订单状态与配送任务状态、记录物流事件，并从Redis的配送员活跃订单集合中移除该订单，确保其不再参与后续调度计算。作为补充，OrderAutoExceptionJob定时任务每5分钟扫描处于待确认状态超时的订单，自动标记为"配送员未响应"。两种检测机制的协作关系如表5-2所示。

表5-2 异常订单检测机制对比

| 检测路径 | 触发方式 | 覆盖场景 |
|---|---|---|
| 配送员主动上报 | 人工触发 | 揽件异常、货物损毁等实时问题 |
| 系统自动检测 | 定时扫描 | 配送员超时未确认、订单超时未进入抢单池 |

管理员在后台通过AdminExceptionOrderController统一管理异常订单，可将可恢复异常重新加入抢单池或取消退款。对于不可恢复异常，系统在取消订单的同时触发退款流程，通过WalletService将金额退回用户钱包，完成从异常发现到资金闭环的完整处理链路。

### 5.2.2 物流跟踪功能实现

物流跟踪是本平台的核心差异化功能，为用户提供配送过程中配送员位置的实时地图可视化。前端使用高德地图组件渲染寄件地址与收件地址的标记点，若订单已进入配送环节，还会叠加展示配送员实时位置的动态标记以及历史轨迹线。地图下方依次展示订单信息卡片、寄收地址详情和物流事件时间轴，实现的界面如图5-2所示。

图5-2 订单物流追踪页面

为实现配送员位置的动态更新，前端封装了useOrderTracking自定义Hook，统一管理WebSocket连接订阅和位置数据更新。组件挂载时自动通过WebSocket订阅对应订单的实时位置频道，服务端推送新数据后，Hook递增更新轨迹数组驱动地图组件平滑移动配送员标记。核心订阅逻辑如源代码5-5所示。

源代码5-5 订单实时追踪WebSocket订阅

```javascript
webSocketService.connect();
webSocketService.subscribeToOrderTracking(orderId, (data) => {
    if (data && data.type === 'location_update') {
        setCurrentLocation({
            latitude: data.latitude, longitude: data.longitude,
            speed: data.speed, direction: data.direction,
        });
        setRoute(prev => [...prev, {
            latitude: data.latitude, longitude: data.longitude,
            createTime: data.timestamp
        }]);
    }
});
// 定时兜底：WebSocket不可用时由轮询补充
refreshInterval.current = setInterval(() => {
    fetchLatestLocation(); fetchRoute();
}, interval);
```

后端TrackServiceImpl在接收配送员上报的位置数据时，执行两级预处理管线：首先通过卡尔曼滤波对GPS原始坐标进行降噪，融合当前观测值与历史估计值得到最优估计坐标；其次进行静止点过滤，计算滤波后坐标与上一有效点的Haversine距离，小于5米则丢弃。滤波器的核心递推算法如源代码5-6所示。

源代码5-6 卡尔曼滤波递推算法

```java
public static KalmanState filter(KalmanState prevState, double newLat, double newLng) {
    if (prevState == null) return new KalmanState(newLat, 1.0, newLng, 1.0);
    // 纬度滤波
    double latP = prevState.latP + DEFAULT_Q;
    double latK = latP / (latP + DEFAULT_R);
    double latX = prevState.latX + latK * (newLat - prevState.latX);
    // 经度滤波
    double lngP = prevState.lngP + DEFAULT_Q;
    double lngK = lngP / (lngP + DEFAULT_R);
    double lngX = prevState.lngX + lngK * (newLng - prevState.lngX);
    return new KalmanState(latX, latP - latK * latP, lngX, lngP - lngK * lngP);
}
```

滤波后的有效位置点通过SimpMessagingTemplate以WebSocket消息发布到STOMP频道，同时追加到Redis轨迹列表。Redis到MySQL的轨迹同步通过定时任务批量写入，实现缓存与持久存储的最终一致。

### 5.2.3 配送调度功能实现

配送调度功能负责在订单支付完成后将未分配配送员的订单匹配到最合适的配送员。系统通过自动派单与抢单两种模式协同工作：当指定区域内有在线且空闲的配送员时，启动自动派单算法；未找到合适配送员时，将订单加入抢单池供配送员自主抢取。配送员进入抢单大厅后，前端通过GPS定位获取当前坐标并进行逆地理编码解析城市信息，向后端请求该城市的可抢订单列表，以卡片形式按距离排列展示，每张卡片标注订单号、寄收地址摘要和运费金额，实现的界面如图5-3所示。

图5-3 配送员抢单大厅页面

抢单大厅的前端实现使用定时轮询和页面焦点监听相结合的方式保证数据时效性：每30秒自动静默刷新，页面获得焦点时也触发刷新，核心逻辑如源代码5-7所示。

源代码5-7 抢单大厅数据刷新逻辑

```javascript
const fetchOrders = useCallback(async () => {
    let lat = currentLocation?.latitude, lng = currentLocation?.longitude;
    if (lat == null || lng == null) {
        const resolved = await getCurrentLocation();
        if (resolved) { lat = resolved.latitude; lng = resolved.longitude; }
    }
    const data = await courierApi.getGrabableOrders(currentCity, lat, lng, 20);
    setOrders(data || []);
}, [currentCity, currentLocation]);

// 30秒定时轮询 + 焦点监听双驱动
useEffect(() => {
    const interval = setInterval(fetchOrders, 30000);
    return () => clearInterval(interval);
}, [fetchOrders]);
```

自动派单方面，后端DispatchServiceImpl的selectBestCourier方法按距离排序后截取前K位候选配送员，对每位逐一计算顺路附加里程D_r、直线距离D_d和当前负载n，评分公式S = w_r·D_r + w_d·D_d + w_l·n，选取评分最低者。核心场景评分逻辑如源代码5-8所示。

源代码5-8 自动派单三位复评分逻辑

```java
List<CourierCandidate> sorted = couriers.stream()
        .sorted(Comparator.comparingDouble(cc -> cc.directKm)).toList();
List<CourierCandidate> toScore = sorted.size() > maxK
        ? new ArrayList<>(sorted.subList(0, maxK)) : sorted;

User best = null;
double bestScore = Double.MAX_VALUE;
for (CourierCandidate cc : toScore) {
    int orderCount = courierStatusService.getCurrentOrderCount(cc.courier.getId());
    double detourKm = computeRouteDetourKmCached(...);
    double score = routeW * detourKm + directW * cc.directKm + loadW * orderCount;
    if (score < bestScore) { bestScore = score; best = cc.courier; }
}
return best;
```

在无候选配送员的情况下，系统按"5公里→15公里→全城"三级降级策略逐步扩大搜索范围。抢单模式通过Redis的Set数据结构按城市-区域维度管理抢单池，配送员抢单时通过SETNX命令获取独占锁以防止同一订单被多人抢到，锁的超时时间设为30秒，获取锁成功后校验订单仍在抢单池且配送员未达最大接单数，校验通过后在事务内创建配送任务并更新订单状态，核心的锁竞争逻辑如源代码5-9所示。

源代码5-9 抢单分布式锁核心逻辑

```java
String lockKey = GRAB_LOCK_PREFIX + orderId;
Boolean locked = redisTemplate.opsForValue()
        .setIfAbsent(lockKey, courierId.toString(), 30, TimeUnit.SECONDS);
if (!Boolean.TRUE.equals(locked)) return "订单正在被其他骑手抢单，请稍后再试";
try {
    Order order = orderMapper.selectById(orderId);
    if (order.getCourierId() != null) return "该订单已被其他骑手接单";
    if (!OrderStatus.PAID.getCode().equals(order.getStatus())) return "订单状态异常";
    // 创建配送任务、更新订单状态...
} finally { redisTemplate.delete(lockKey); }
```

抢单成功后系统通过NotificationCopy通知下单用户配送员已接单。若配送员拒绝已分配的订单，订单回到已支付状态自动重新进入抢单池，保证调度弹性。

### 5.2.4 消息通知功能实现

消息通知模块在订单状态变更、配送进度更新等关键业务节点自动向相关用户生成通知消息。前端消息中心以FlatList按时间倒序分页展示通知列表，未读消息以浅蓝色背景和红色圆点标记区分，消息类型图标按订单、投诉、系统和活动四类分别渲染不同颜色。用户可点击进入详情查看完整内容，支持逐条已读、全部已读和滑动删除操作。实现的界面如图5-4所示。

图5-4 用户端消息通知中心页面

后端方面，通知文案通过NotificationCopy类集中维护，以静态工厂方法的形式提供各业务场景的标题和正文模板。下发策略保证同一业务动作仅对需要异步知晓的一方写入消息中心——配送员接单通知仅写给用户而不在配送员消息中心重复出现（配送员侧通过实时通道获知）。调用方传入订单号、配送员姓名等动态参数即可获得完整文案，如源代码5-10所示。

源代码5-10 通知文案模板与统一发送

```java
public static String grabOrderUserTitle() {
    return "配送员已接单，即将与您联系";
}
public static String grabOrderUserBody(String orderNo, String courierName) {
    String name = StringUtils.hasText(courierName) ? courierName : "配送员";
    return "好消息：您的订单已由配送员接单。\n"
            + "【订单号】" + orderNo + "\n【为您服务】" + name;
}

public void sendNotification(Long userId, String userType,
        String type, String title, String content) {
    Notification n = new Notification();
    n.setUserId(userId); n.setUserType(userType);
    n.setType(type); n.setTitle(title); n.setContent(content); n.setIsRead(0);
    notificationMapper.insert(n);
}
```

这种"模板集中定义、发送统一调用"的设计将文案维护收敛到单一类中，业务服务仅需关心"何时发"和"发给谁"，无需嵌入手写文案字符串。

### 5.2.5 用户账号功能实现

系统用户分为普通用户、配送员和管理员三种角色。前端登录页面提供用户名和密码输入框，调用AuthContext中的login方法完成认证。注册页面通过角色切换按钮区分普通用户与配送员注册路径，配送员注册时展示额外的身份认证、车辆信息、紧急联系人和工作城市四个分组表单，支持身份证照片上传和省市区三级联动城市选择，实现的界面如图5-5所示。

图5-5 系统登录与配送员注册界面

登录的前端实现将认证逻辑封装在AuthContext中，登录成功后自动存储访问令牌和刷新令牌，后续请求由axios拦截器自动携带Bearer令牌。注册表单具备完善的前端校验逻辑，配送员角色的表单根据选择的车辆类型动态显示不同的必填字段。核心实现如源代码5-11所示。

源代码5-11 登录与角色差异化注册的前端实现

```javascript
// AuthContext 登录逻辑
const login = async (username, password) => {
    const response = await authApi.login(username, password);
    const { accessToken, refreshToken, role } = response;
    await AsyncStorage.setItem('accessToken', accessToken);
    await AsyncStorage.setItem('refreshToken', refreshToken);
    return { success: true, role };
};

// 注册表单的角色差异化校验
const validateForm = () => {
    if (!formData.username.trim()) return false;
    if (formData.password.length < 6) return false;
    if (formData.password !== formData.confirmPassword) return false;
    if (formData.role === 'delivery') {
        if (!formData.idCardNo.trim()) return false;
        if (!formData.idCardFront) return false;
        if (!formData.workCity.trim()) return false;
    }
    return true;
};
```

后端认证方面，系统采用"双令牌"机制保障会话安全性。登录时服务端校验密码摘要后分别生成访问令牌和刷新令牌，两种令牌使用独立的签名密钥和过期时间——访问令牌有效期1小时，刷新令牌有效期7天——生成后存入Redis供验证与失效管理。令牌生成的核心逻辑如源代码5-12所示。

源代码5-12 JWT双令牌生成逻辑

```java
public String generateAccessToken(String username, String role) {
    Map<String, Object> claims = new HashMap<>();
    claims.put("role", role); claims.put("type", "access");
    String token = Jwts.builder()
            .claims(claims).subject(username)
            .issuedAt(new Date())
            .expiration(new Date(System.currentTimeMillis() + accessExpiration))
            .signWith(getAccessSigningKey()).compact();
    tokenRedisService.storeAccessToken(token, username, accessExpiration);
    return token;
}
```

请求拦截方面，JwtAuthenticationFilter从请求头提取Bearer令牌，校验通过后将认证信息写入Spring Security的安全上下文；校验失败直接返回401状态码阻断未授权请求。当访问令牌过期时，客户端自动携带刷新令牌完成续期，实现无感知的会话保持。登出时系统将令牌加入Redis黑名单，确保旧令牌即便签名合法也被拒绝。安全配置方面，SecurityConfig类将系统设为无状态会话模式、禁用CSRF保护，对认证、文件上传和WebSocket路径放行匿名访问，其余接口均要求携带有效JWT令牌。

### 5.3 系统构建与部署实现

系统后端基于Docker进行容器化构建，通过多阶段构建（Multi-stage Build）技术将构建过程与运行环境分离。Dockerfile设计了build和runtime两个阶段：build阶段使用Maven镜像编译Java源码并打包为可执行JAR文件，runtime阶段使用精简的OpenJDK JRE镜像，仅复制build阶段产出的JAR文件，避免将Maven、JDK等编译工具链带入运行镜像。由于运行镜像不包含编译工具，镜像体积有效减小，有利于加快镜像拉取和容器启动速度。后端Dockerfile的核心内容如源代码5-17所示。

源代码5-17 后端Docker多阶段构建配置

```dockerfile
FROM maven:3.9-eclipse-temurin-17 AS build
WORKDIR /app
COPY pom.xml .
RUN mvn dependency:go-offline -B
COPY src ./src
RUN mvn package -DskipTests -B

FROM eclipse-temurin:17-jre-alpine AS runtime
WORKDIR /app
COPY --from=build /app/target/*.jar app.jar
EXPOSE 8082
ENTRYPOINT ["java", "-jar", "app.jar"]
```

系统的生产环境部署需要以下基础设施：云服务器用于运行Docker容器承载后端服务，云数据库提供托管的MySQL服务，云缓存服务替代自建Redis，云消息队列服务替代自建RabbitMQ，对象存储服务用于文件上传，以及CDN用于静态资源加速。本文选用阿里云作为云服务提供商——一方面，系统已在开发阶段接入阿里云OSS实现文件上传功能，保持云服务商的统一有助于降低运维复杂度；另一方面，阿里云在国内的可用区覆盖广泛，能够为物流配送场景提供低延迟的服务访问。系统部署的整体架构如图5-6所示。

图5-6 系统部署整体架构图

部署的第一步是在阿里云控制台创建所需的各项云资源。在云服务器ECS方面，创建一台配置为2核4GB内存的ECS实例，操作系统选用Alibaba Cloud Linux 3，并开启80和443端口用于HTTP和HTTPS访问。在数据库方面，创建ApsaraDB RDS for MySQL实例，将开发环境的数据库表结构通过初始化脚本导入，并配置白名单仅允许ECS实例的内网IP访问。在缓存和消息队列方面，分别创建ApsaraDB for Redis实例和RocketMQ实例，将后端配置文件application.yml中的Redis连接地址和RabbitMQ连接信息替换为对应的云服务内网地址。在对象存储方面，沿用开发阶段已创建的OSS Bucket，为其绑定自定义域名并配置CDN加速。云资源创建完成后的控制台概览如图5-7所示。

图5-7 阿里云资源创建——控制台概览

云资源就绪后，进入后端服务的部署环节。首先，在项目根目录执行Docker构建命令生成后端镜像，并为镜像标记版本号。其次，在阿里云容器镜像服务ACR中创建镜像仓库，使用Docker CLI将本地构建的镜像推送至ACR仓库。最后，登录ECS实例，执行Docker拉取命令从ACR获取最新版本镜像，通过docker run命令启动容器，挂载宿主机的application.yml配置文件以覆盖镜像内默认配置，并设置容器随系统自动重启。部署完成后，可通过ECS公网IP的8082端口验证后端API服务的可访问性。后端服务部署的关键步骤如图5-8所示。

图5-8 后端服务镜像构建与容器部署

后端服务部署完成后，需要配置HTTP访问服务使Android客户端能够通过公网访问API。在ECS实例上通过Docker部署Nginx作为反向代理，将所有到达80和443端口的请求代理转发至后端Spring Boot容器的8082端口。在域名方面，为系统申请一个专属域名，在阿里云SSL证书服务中申请免费的SSL证书，在Nginx中配置证书路径并开启强制HTTPS跳转，确保客户端与服务器之间的数据传输加密。域名解析方面，在DNS提供商处添加域名的A记录，将域名指向ECS实例的公网IP地址。同时，Nginx针对WebSocket协议进行特殊配置，为路径"/api/ws-location"设置代理头Upgrade和Connection以支持WebSocket连接升级，确保物流跟踪的WebSocket推送功能在反向代理环境下正常工作。HTTP访问服务配置完成后，Android客户端即可通过域名访问后端服务。HTTP访问与Nginx反向代理配置如图5-9所示。

图5-9 HTTP访问服务——Nginx反向代理与SSL配置

最后是Android客户端的构建与分发。前端项目使用React Native框架开发，执行Gradle构建命令生成APK安装包。在构建release版本前，需要将前端代码中硬编码的API地址替换为正式环境的域名，并确保AndroidManifest.xml中的网络安全性配置允许访问该域名。构建完成后，可将APK文件上传至系统官网或应用分发平台供用户下载安装。Android客户端构建与分发流程如图5-10所示。

图5-10 Android客户端构建与分发

### 5.4 本章小结

本章按照第4章的系统设计方案，实现了基于App的物流管理和追踪平台的核心功能。项目整体配置方面，通过Maven POM文件管理后端依赖，通过application.yml集中管理数据库、缓存、消息队列和派单规则等各项运行参数。系统功能实现方面，重点完成了订单处理与异常恢复、物流跟踪、配送调度、消息通知和用户账号五个模块的编码工作。

订单处理与异常恢复模块从有限状态机模型出发，定义了订单从待支付到已收货共十种状态的流转规则，在此基础上实现了订单号生成、细粒度权限校验和配送员隐私保护等正常流转功能，同时以物品交接状态为分类依据将异常分为7种类型并约束对应的恢复动作，通过配送员主动上报和定时任务自动检测的双路径机制实现异常的及时发现与处置。物流跟踪模块将客户端GPS采集、卡尔曼滤波降噪、静止点过滤、Redis缓存和WebSocket实时推送串联为一条完整的技术通道，使用户能够在地图上直观查看配送员实时位置和历史轨迹。配送调度模块构建了基于距离、顺路性和负载三维度评分的自动派单算法，并配合基于Redis Set与分布式锁的抢单机制，使系统在自动与手动两种模式间灵活切换。消息通知模块通过NotificationCopy集中管理文案模板、NotificationServiceImpl统一发送存储，在订单状态变更和配送进度更新等关键节点自动向相关用户生成通知消息。用户账号模块基于JWT双令牌机制实现了登录认证、令牌刷新、登出失效和配送员注册审核等安全功能，结合Spring Security的无状态会话配置构建了完整的访问控制体系。以上功能的实现共同支撑了物流配送平台在客户端下单、支付、运单追踪、配送调度、异常处理和签收确认等完整业务流程。
